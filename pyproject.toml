[tool.black]
line-length = 88

[tool.ruff]
line-length = 88

[tool.isort]
profile = "black"

[tool.pytest.ini_options]
markers = [
    "integration: tests that use real external components",
]
testpaths = ["tests"]
pythonpath = ["."]